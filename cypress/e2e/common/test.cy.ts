import { classNames } from "@/shared/lib/classNames/classNames";

describe("test.cy.ts", () => {
    it("playground", () => {
        //cy.mount();
        cy.visit("https://example.cypress.io");
    });
});

export {};
